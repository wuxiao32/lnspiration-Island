<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.inspiration.island.mapper.UserMapper">

    <select id="selectByUsername" resultType="com.inspiration.island.entity.User">
        SELECT id, username, password FROM user WHERE username = #{username}
    </select>

    <select id="selectByUsernameAndPassword" resultType="com.inspiration.island.entity.User">
        SELECT id, username, password FROM user
        WHERE username = #{username} AND password = #{password}
    </select>

    <select id="countInspirationByUserId" resultType="int">
        SELECT COUNT(*) FROM inspiration
        WHERE user_id = #{userId} AND (is_delete IS NULL OR is_delete = 0)
    </select>

    <select id="countCommentByUserId" resultType="int">
        SELECT COUNT(*) FROM comment
        WHERE user_id = #{userId}
    </select>

</mapper>
