<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.inspiration.island.mapper.CollectRecordMapper">

    <update id="updateCancel">
        UPDATE collect_record
        SET is_cancel = 1
        WHERE user_id = #{userId} AND inspiration_id = #{inspirationId}
    </update>

    <select id="selectValid" resultType="com.inspiration.island.entity.CollectRecord">
        SELECT id, user_id, inspiration_id, create_time, is_cancel
        FROM collect_record
        WHERE user_id = #{userId} AND inspiration_id = #{inspirationId} AND is_cancel = 0
    </select>

    <select id="countValidByInspirationId" resultType="int">
        SELECT COUNT(*) FROM collect_record
        WHERE inspiration_id = #{inspirationId} AND is_cancel = 0
    </select>

</mapper>
